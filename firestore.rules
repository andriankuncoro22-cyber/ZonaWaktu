rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Mengizinkan akses publik sesuai permintaan "tanpa login" untuk operasional ZONA WAKTU
      allow read, write: if true;
    }
  }
}